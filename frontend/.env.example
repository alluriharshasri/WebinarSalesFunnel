# ================================
# WEBINAR SALES FUNNEL - FRONTEND CONFIG
# ================================

# API Base URL
# Development: /api (proxied to backend via package.json proxy setting)
# Production: https://your-backend-domain.com/api
REACT_APP_API_URL=/api

# ================================
# NOTE: Google Sheets Configuration
# ================================
# All Google Sheets IDs, GIDs, and CSV URLs are managed in the backend
# They are fetched dynamically from /api/config/google-sheets endpoint
# To update Sheet IDs or GIDs, modify backend/.env file
# ================================

# ================================
# PRODUCTION DEPLOYMENT
# ================================
# For production deployment:
# 1. Update REACT_APP_API_URL to your backend API URL
#    Example: REACT_APP_API_URL=https://api.yourdomain.com/api
# 2. Ensure backend CORS settings allow your frontend domain
# 3. Build the app: npm run build
# 4. Deploy the build folder to your hosting service
# ================================
